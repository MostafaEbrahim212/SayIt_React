const initialState = {
  profile: null,
  status: 'idle',
  error: null,
};

export default function profileReducer(state = initialState, action) {
  switch (action.type) {
    default:
      return state;
  }
}

export const profileActions = {};
